# apps/coc-gateway — placeholder

Standalone Clash of Clans gateway service (isolates the official CoC API key and
the egress proxy). Scaffolded in **issue #4** (`feat(coc-gateway): standalone
gateway service skeleton …`).

Until then this directory is an intentional placeholder so the workspace layout
is fixed. It has no `package.json` yet, so pnpm does not treat it as a workspace
package.
