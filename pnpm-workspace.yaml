packages:
  - "apps/*"
  - "packages/*"

# pnpm blocks dependency build (postinstall) scripts by default for supply-chain
# safety. esbuild (pulled in by vitest) needs its install script to materialize
# the platform binary; allow only that one explicitly.
onlyBuiltDependencies:
  - esbuild
